import Foundation

enum PrivacyContent {
    static let statements: [String] = [
        "Your reminders are stored locally on your device.",
        "LifeCue does not require an account or login.",
        "Backup files are created only when you choose to export a backup.",
        "Calendar access is used only for Calendar features that require it.",
        "Screenshot & photo extraction extracts reminder details from images. Results may not always be accurate, so review the details before creating a reminder."
    ]
}
